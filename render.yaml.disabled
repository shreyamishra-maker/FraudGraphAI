Render Blueprint disabled temporarily; configure the existing Render service directly with Python runtime, root directory backend, build `pip install -r requirements.txt`, start `uvicorn app.main:app --host 0.0.0.0 --port $PORT`.